# 2026-06-02

## Deployment Flow Saved
Karan shared his deployment pipeline. Saved to MEMORY.md permanently:
1. GitHub push → GitHub Actions (`deploy.yml`) → Docker Hub images
2. Portainer stack (compose + env vars) pulls latest from Docker Hub

No need to ask about this again — it's in MEMORY.md.